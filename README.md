# Sench//Index Fashion Magazine

Modern Street Archive fashion magazine website built with React, TypeScript, Vite, and CSS.

## Scripts

```bash
npm run dev
npm run build
npm run deploy:root
```

`npm run deploy:root` builds the Vite app and syncs the generated multi-page HTML files and `assets/` for GitHub Pages hosting.
