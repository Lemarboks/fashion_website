# Sench//Index Fashion Magazine

Modern South African fashion magazine website built with React, TypeScript, Vite, and CSS.

## Scripts

```bash
npm run dev
npm run build
npm run deploy:root
```

`npm run deploy:root` builds the Vite app and syncs the generated `index.html`, `404.html`, `view.html`, and `assets/` files for GitHub Pages hosting.
